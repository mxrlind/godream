import { CreatorDashboard } from '@/features/creator/CreatorDashboard';

export const metadata = { title: 'Creator Studio — GoDream' };

export default function Page() {
  return <CreatorDashboard />;
}
